{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalDependencies": [".env"],
  "tasks": {
    "build": {
      "dependsOn": ["^lint", "^check-types", "^bundle", "^build"],
      "env": [
        "PRISMA_DATABASE_URL",
        "MASTRA_DATABASE_URL",
        "MASTRA_WORKSPACE_PATH",
        "AIHUBMIX_API_KEY",
        "DEV_PORT"
      ],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "dependsOn": ["@mocean/mastra#build:client"],
      "cache": false,
      "inputs": [".env"],
      "persistent": true
    },
    "build:client": {
      "cache": true,
      "outputs": ["dist/**"]
    },
    "bundle": {
      "dependsOn": ["^bundle"],
      "cache": true,
      "outputs": ["dist/**"]
    },
    "serve": {}
  }
}
